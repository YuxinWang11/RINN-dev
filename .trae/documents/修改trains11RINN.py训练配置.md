## 修改计划

### 1. 数据加载逻辑修改
- **训练集**：仅加载 `data/S Parameter Plot200.csv` 和 `data/S Parameter Plot300.csv`
- **验证集**：仅加载 `data/S Parameter Plot1perfect.csv`
- **修改点**：
  - 分离训练集和验证集的数据加载过程
  - 确保验证集只包含一个元素
  - 保持数据预处理和归一化逻辑不变

### 2. 训练配置修改
- **训练轮数**：将 `num_epochs` 从 300 改为 200
- **修改点**：
  - 更新配置字典中的 `num_epochs` 值
  - 确保其他训练参数保持不变

### 3. 代码结构调整
- **修改点**：
  - 调整数据加载部分的代码结构，使其更清晰地分离训练集和验证集
  - 保持其他功能不变，确保模型训练和评估逻辑正常运行

### 4. 验证修改效果
- **验证点**：
  - 确保训练集只包含来自 Plot200 和 Plot300 的数据
  - 确保验证集只包含来自 Plot1perfect 的单个元素
  - 确保训练轮数设置为 200
  - 确保模型能够正常训练和评估

### 5. 预期结果
- 模型使用 Plot200 和 Plot300 的数据进行 200 轮训练
- 模型使用 Plot1perfect 的数据作为验证集
- 训练完成后生成相应的模型文件和评估结果